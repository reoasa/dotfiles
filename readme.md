## Install

    $ brew install tmux
    $ brew install peco
    $ brew install reattach-to-user-namespace

## Setup

    $ sh download.sh
    $ sh linkdotfiles.sh
    $ mkdir ~/.vim/colors
    $ cp solarized/solarized.vim ~/.vim/colors
